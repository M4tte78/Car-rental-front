import { BrandColorPipe } from './brand-color.pipe';

describe('BrandColorPipe', () => {
  it('create an instance', () => {
    const pipe = new BrandColorPipe();
    expect(pipe).toBeTruthy();
  });
});
